name: Update NASDAQ-100 Data

on:
  schedule:
    # Läuft wöchentlich am Sonntag um 06:00 UTC
    - cron: '0 6 * * 0'
  workflow_dispatch: # Ermöglicht manuellen Start

jobs:
  update-and-create-pr:
    runs-on: ubuntu-latest
    # Berechtigungen für das Schreiben von Inhalten und das Erstellen von PRs
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run NASDAQ-100 scraper
        id: scraper
        run: |
          python nasdaq100_scraper.py

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          # Konfiguration für den Commit und den Pull Request
          commit-message: "chore: Update NASDAQ-100 data"
          title: "🤖 chore: Update NASDAQ-100 data"
          body: |
            Automatische Aktualisierung der NASDAQ-100 Daten.

            Dieser Pull Request wurde von einer GitHub Action erstellt und enthält die neuesten Daten. Bitte mergen, um die Änderungen zu übernehmen und ein neues Release zu erstellen.
          # Branch, der für den PR erstellt wird
          branch: chore/update-nasdaq-data
          # Ziel-Branch, in den gemerged werden soll
          base: master
          # Löscht den Branch automatisch nach dem Mergen
          delete-branch: true
